{% if section.blocks.size > 0 %}
<div class="announcement-bar full-width color-{{ section.settings.color_scheme }}" data-interval="{{ section.settings.slide_time | times: 1000 }}">
  <div class="announcement-slider">

    {% for block in section.blocks %}
      <div class="announcement-slide" {{ block.shopify_attributes }}>
        {% if block.settings.link != blank %}
          <a href="{{ block.settings.link }}">
        {% endif %}
          <span>{{ block.settings.text }}</span>
        {% if block.settings.link != blank %}
          </a>
        {% endif %}
      </div>
    {% endfor %}

  </div>

  {% if section.settings.show_arrows %}
  <button class="announcement-prev">‹</button>
  <button class="announcement-next">›</button>
  {% endif %}
</div>
{% endif %}

<style>
.announcement-bar {
  background-color: var(--color-background);
  color: var(--color-foreground);
  font-size: {{ section.settings.font_size }}px;
  font-weight: {% if section.settings.font_bold %}700{% else %}400{% endif %};
  font-style: {% if section.settings.font_italic %}italic{% else %}normal{% endif %};
  text-align: center;
  position: relative;
  overflow: hidden;
  transition: background-color 0.3s ease, color 0.3s ease;
  z-index: 1001;
}

/* Make announcement bar sticky when header is sticky */
body.has-sticky-header .announcement-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  width: 100%;
  z-index: 1001;
}

.announcement-slider {
  display: flex;
  transition: transform 0.5s ease;
}

.announcement-slide {
  min-width: 100%;
  padding: 10px 20px;
}

.announcement-slide a {
  color: inherit;
  text-decoration: none;
}

.announcement-prev,
.announcement-next {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: none;
  color: var(--color-foreground);
  border: none;
  font-size: 18px;
  cursor: pointer;
  transition: color 0.3s ease, opacity 0.3s ease;
}

.announcement-prev:hover,
.announcement-next:hover {
  opacity: 0.7;
}

.announcement-prev { left: 10px; }
.announcement-next { right: 10px; }
</style>

<script>
document.addEventListener("DOMContentLoaded", function () {
  const bar = document.querySelector(".announcement-bar");
  if (!bar) return;

  const slides = bar.querySelectorAll(".announcement-slide");
  const slider = bar.querySelector(".announcement-slider");
  const interval = parseInt(bar.dataset.interval);
  let index = 0;

  function showSlide(i) {
    slider.style.transform = `translateX(-${i * 100}%)`;
  }

  function next() {
    index = (index + 1) % slides.length;
    showSlide(index);
  }

  let timer = setInterval(next, interval);

  const prevBtn = bar.querySelector(".announcement-prev");
  const nextBtn = bar.querySelector(".announcement-next");

  if (prevBtn) {
    prevBtn.addEventListener("click", () => {
      clearInterval(timer);
      index = (index - 1 + slides.length) % slides.length;
      showSlide(index);
    });
  }

  if (nextBtn) {
    nextBtn.addEventListener("click", () => {
      clearInterval(timer);
      next();
    });
  }
});
</script>

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [
     {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "slide_time",
      "label": "Slide change (seconds)",
      "min": 2,
      "max": 10,
      "step": 1,
      "default": 4
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show navigation arrows",
      "default": true
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "font_size",
      "label": "Font size",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 13
    },
    {
      "type": "checkbox",
      "id": "font_bold",
      "label": "Bold text",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "font_italic",
      "label": "Italic text",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "announcement",
      "name": "Announcement",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Announcement text",
          "default": "Free shipping on orders above ₹999"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Optional link"
        }
      ]
    }
  ],
  "max_blocks": 5,
  "presets": [
    {
      "name": "Announcement Bar",
      "blocks": [
        { "type": "announcement", "settings": { "text": "Free shipping on orders above ₹999" }},
        { "type": "announcement", "settings": { "text": "Republic Day Sale – Flat 20% OFF" }},
        { "type": "announcement", "settings": { "text": "Extra 10% off on prepaid orders" }}
      ]
    }
  ]
}
{% endschema %}